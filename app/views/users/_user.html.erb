<h2>New book</h2>
<% if users.errors.any? %>
<%= users.errors.count %>件のエラーが発生した。
<ul>
  <% users.errors.full_messages.each do |message| %>
  <li><%= message %></li>
  <% end %>
</ul>
<% end %>

<%= form_with model: book, url: book_path, local:true do |f| %>
<label>Title</label>
<%= f.text_field :title %>
<label>Opinion</label>
<%= f.text_area :body %>

<%= f.submit 'Create Book' %>
<% end %>