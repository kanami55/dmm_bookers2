<h2>User info</h2>


<table>
  <tbody>
    <tr></tr>
    <tr>
      <td>name</td>
      <td><%= @user.name %></td>
    </tr>
    <tr>
      <td>introduction</td>
      <td><%= @user.introduction%></td>
    </tr>
  </tbody>
</table>

<div class="row">
<%=link_to'●', edit_user_path %>
</div>

<h2>New book</h2>
<% if @user.errors.any? %>
<%= @user.errors.count %>件のエラーが発生した。
<ul>
  <% @user.errors.full_messages.each do |message| %>
  <li><%= message %></li>
  <% end %>
</ul>
<% end %>

<%= form_with model: @users, local:true do |f| %>
<label>Title</label>
<%= f.text_field :title %>
<label>Opinion</label>
<%= f.text_area :body %>

<%= f.submit 'Create Book', book_path(@books) %>
<% end %>

<h2>Books</h2>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Opinion</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%=  %></td>
      <td><%=  %></td>
      <td><%=  %></td>
    </tr>
  </tbody>
</table>
